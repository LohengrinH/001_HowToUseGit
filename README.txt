이전 - 깃 설치 및 깃헙 가입 후 Bash로 실행
-----------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------

1. 초기 설정

git config --global user.name "이름"
       // ""까지 입력해야 한다.
git config --global user.email "깃허브 메일주소" // 매번 물어보는 귀찮음을 피하기 위해 설정.



-----------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------

2. 저장 폴더 만들기 (Ex. D드라이브에 git 폴더 생성)

cd d: 		//d드라이브 이동
mkdit ./git 	// "mkdir ./폴더명" 으로 디렉토리 생성

mkdir ~/MyProject   // c 드라이브에서 로컬 디렉토리 만드는 명령어

-----------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------

3. 명령어 사용할 수 있는 디렉토리로 변환
git init		// 버전 관리를 위한 도구들이 모여있는 폴더 생성

-----------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------

4. 만든 저장 폴더로 접근해서 파일 만들기

cd d:\git 		// 아무런 디렉토리에도 접속하지 않앗을때 한번에 해당 디렉토리로 이동한다.

echo "test md" >> "$(date +%y)$(date +%m)$(date +%d)_(date +%H)$(date +%M)_"README.md		// test md 라는 내용이 들어있는 현재 시각의 README 파일을 생성한다.

 
-----------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------

5. 만든 파일 확인(폴더 확인) 및 Tracked(관리 대상 파일)상태 변환

git status		// 현재 폴더의 상태를 보는 것, 자료를 추가했다면 빨간색으로 뜰것

git add <파일이름> 		// 관리 대상 파일 상태로 만드는 명령어, git add * 을 입력시 폴더 전체를 가리킴

git status 		// 입력시 녹색으로 변화된것 확인

git commit -m "ADD README.txt"	// 커밋 (변경사항 있을시에도 커밋 후 푸쉬해야한다.)

-----------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------

6. 로컬 저장소와 깃허브 저장소 연결하기

git remote add origin https://github.com/LohengrinH/GitStart.git // 로컬과 원격 저장소를 연결한다.


git remote -v // 연결상태를 확인한다.


git push		// 원격 저장소로 변경사항을 업로드나 push

위에것 안될시 
git push -u origin master	// 내 저장소의 master 브랜치를 지정하도록하는 것

git remote remove origin - 원격 삭제



-----------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------
7. 기타 명령어

git remote rename nowname newname		// 리모트 저장소 이름 변경

git remote rm 저장소 이름		// 리모트 저장소 삭제

git push <remote-name> --delete <branch-name>  //리모트 브랜치 삭제

-----------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------

8. 기타 사항
깃의 저장소 Settings 탭 메뉴에서 Repository name을 변경하면 깃 저장소 url도 변경되기 때문이 이를 반영해 주어야 한다.

git remote set-url origin https://github.com/LohengrinH/2_Git.git		// 으로 변경


-----------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------